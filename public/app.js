const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function get(u){const r=await fetch(u);if(!r.ok)throw new Error((await r.json()).error||'Request failed');return r.json()}
async function loadSignal(){const symbol=$('symbol').value;$('hero').innerHTML='<div class="card">Loading market intelligence…</div>';try{const [s,e,n]=await Promise.all([get('/api/signal?symbol='+symbol),get('/api/calendar'),get('/api/news?symbol='+symbol)]);renderSignal(s);renderCalendar(e);renderNews(n);loadModel();}catch(err){$('hero').innerHTML='<div class="card">'+esc(err.message)+'</div>'}}
function renderSignal(s){const cls=s.direction==='LONG'?'long':s.direction==='SHORT'?'short':'wait';$('hero').innerHTML=`<div class="card"><span class="label">${esc(s.symbol)} · AI synthesis</span><div class="big">${s.price.toFixed(s.price>100?2:5)}</div><div class="direction ${cls}">${esc(s.direction)} · ${(s.probability*100).toFixed(1)}% probability</div></div><div class="card"><span class="label">Confidence</span><div class="big">${(s.confidence*100).toFixed(0)}%</div><span class="label">model conviction</span></div><div class="card"><span class="label">RSI 14</span><div class="big">${s.features.rsi.toFixed(1)}</div><span class="label">momentum</span></div><div class="card"><span class="label">Event risk</span><div class="big">${(s.eventRisk*100).toFixed(0)}%</div><span class="label">next 24h</span></div>`;$('updated').textContent=new Date(s.generatedAt).toLocaleTimeString();$('factors').innerHTML=s.explanation.map((x,i)=>`<div class="factor"><b>${['Trend','Momentum','MACD','Risk'][i]||'Context'}</b><span>${esc(x)}</span></div>`).join('')}
function renderCalendar(a){$('calendar').innerHTML=a.slice(0,8).map(e=>`<div class="event"><b>${new Date(e.time).toLocaleDateString(undefined,{month:'short',day:'numeric'})}</b><span>${esc(e.event)}<br><small>${esc(e.currency||e.country||'')}</small></span><span class="impact">${esc(e.impact)}</span></div>`).join('')||'<div class="event">No events returned.</div>'}
function renderNews(a){$('news').innerHTML=a.slice(0,7).map(n=>`<div class="article"><b>${esc(n.headline)}</b><small>${esc(n.source||'')} · ${n.time?new Date(n.time).toLocaleString():''}</small></div>`).join('')}
async function loadModel(){try{const x=await get('/api/model/status');$('modelStatus').textContent=x.trained?'TRAINED':'BASELINE';$('trainOut').textContent=x.lastModel?JSON.stringify(x.lastModel.metrics,null,2):`${x.observations} labeled observations available.`}catch(e){$('modelStatus').textContent='OFFLINE'}}
async function trainModel(){try{$('trainOut').textContent='Training…';const x=await fetch('/api/model/train',{method:'POST'}).then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);return d});$('trainOut').textContent=JSON.stringify(x.metrics,null,2);loadSignal()}catch(e){$('trainOut').textContent=e.message}}
loadSignal();